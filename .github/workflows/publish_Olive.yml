name: Publish Olive Packages

on:
  push:
    branches: [ generalize-publish-action ]

jobs:
 build:
    strategy:
      matrix:
        configuration: [Olive,Olive.Aws]

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2

    - name: Publish ${{matrix.configuration}} NuGet Package
      uses: brandedoutcast/publish-nuget@v2
      with:
        PROJECT_FILE_PATH: ${{matrix.configuration}}/${{matrix.configuration}}.csproj
        NUGET_KEY: ${{secrets.NUGET_API_KEY}}