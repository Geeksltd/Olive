namespace Olive.Mvc
{
    public enum RecaptchaSize
    {
        Normal,
        Compact
    }

    public enum RecaptchaType
    {
        Image,
        Audio
    }

    public enum RecaptchaTheme
    {
        Light,
        Dark
    }
}
